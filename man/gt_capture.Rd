% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_capture.R
\name{gt_capture}
\alias{gt_capture}
\title{Capture and report errors to GlitchTip}
\usage{
gt_capture(gt, expr, request = NULL)
}
\arguments{
\item{gt}{A \code{gt_connection} object created by \code{\link[=gt_connect]{gt_connect()}}.}

\item{expr}{An expression to evaluate (supports \code{{ }} blocks).}

\item{request}{Optional plumber2 \code{request} object. When provided, the
endpoint path and HTTP method are included in the error report.}
}
\value{
The value of \code{expr}, invisibly. Errors are re-raised after
reporting.
}
\description{
Evaluates \code{expr} and reports any error to GlitchTip before letting it
propagate. Uses \code{withCallingHandlers()} internally so the full call stack
is preserved in the report.
}
\details{
If \code{gt} is an inactive connection (no DSN configured), \code{expr} is evaluated
normally and nothing is reported.
}
\examples{
# With an inactive connection (no DSN), gt_capture() is a transparent no-op
gt <- gt_connect()

result <- gt_capture(gt, 1 + 1)

}
